import { type PaginationParams } from '../hooks/useHuPagination';
/** Converts PaginationParams into a flat object suitable for URL search params. */
export declare const formatSearchParams: (values?: PaginationParams) => {};
