/**
 * Base z-index for drawer layers. MUI's default modal z-index is 1300,
 * so we start from there to ensure drawers stack correctly above other modals.
 */
export const BASE_DRAWER_Z_INDEX = 1300;
/**
 * Border radius for drawer paper component (rounded top-left and bottom-left corners)
 */
export const DRAWER_BORDER_RADIUS = '16px 0 0 16px';
