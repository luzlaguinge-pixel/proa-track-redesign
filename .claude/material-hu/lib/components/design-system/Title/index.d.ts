import { type TitleProps } from './types';
export declare const Title: ({ id, centered, right, copetin, copetinTooltip, description, descriptionTooltip, date, title, variant, withEllipsis, overflow, disabled, fontWeight, sx, slotProps, }: TitleProps) => import("react/jsx-runtime").JSX.Element;
export type { TitleProps };
export default Title;
