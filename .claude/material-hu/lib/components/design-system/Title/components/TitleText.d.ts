import { type TitleTextProps } from '../types';
declare const TitleText: ({ id, title, variant, withEllipsis, overflow, disabled, fontWeight, sx, component, onOverflowChange, onClick, }: TitleTextProps) => import("react/jsx-runtime").JSX.Element | null;
export default TitleText;
