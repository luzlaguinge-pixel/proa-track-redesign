import { type TabsProps } from './types';
declare const Tabs: {
    ({ tabs, sx, onTabChange, defaultValue, value, ...tabsProps }: TabsProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export type { TabsProps };
export default Tabs;
