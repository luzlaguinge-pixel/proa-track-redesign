import { type Theme } from '@mui/material';
import { type SkeletonProps } from './types';
export declare const getBorderRadius: (variant: SkeletonProps["variant"], theme: Theme) => number | "50%" | undefined;
