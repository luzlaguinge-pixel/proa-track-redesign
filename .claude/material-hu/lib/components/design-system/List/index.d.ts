import { type PropsWithChildren } from 'react';
import { type ListProps } from './types';
export declare const List: ({ id, sx, children }: PropsWithChildren<ListProps>) => import("react/jsx-runtime").JSX.Element;
export type { ListProps };
export default List;
