import { type TextAreaProps } from './types';
declare const TextArea: ({ actions, simplifyEditor, visibleActions, content, disabled, error, errorText, handlePaste, helperText, handleDrop, label, onChange, onBlur, onFocus, placeholder, slotProps, success, sx, immediatelyRender, }: TextAreaProps) => import("react/jsx-runtime").JSX.Element;
export type { ActionId, TextAreaProps } from './types';
export default TextArea;
