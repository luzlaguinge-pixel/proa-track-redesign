import { type InputTimeProps } from './types';
declare const InputTime: ({ value, disabled, error, size, label, helperText, errorText, fullWidth, sx, slotProps, onChange, minutesStep, placeholder, timezone, referenceDate, disablePast, disableFuture, minTime, maxTime, noIcon, hideErrorText, ...props }: InputTimeProps) => import("react/jsx-runtime").JSX.Element;
export type { InputTimeProps };
export default InputTime;
