import { type TooltipProps } from './types';
declare const Tooltip: {
    ({ children, disableTooltip, delay, sx, slotProps, open, ...props }: TooltipProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export type { TooltipProps };
export default Tooltip;
