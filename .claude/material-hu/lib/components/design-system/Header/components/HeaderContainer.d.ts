import { type HeaderContainerProps } from './types';
declare const HeaderContainer: ({ children, slotProps }: HeaderContainerProps) => import("react/jsx-runtime").JSX.Element;
export default HeaderContainer;
