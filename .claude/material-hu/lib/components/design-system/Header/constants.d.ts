export declare const MAX_MAIN_ACTIONS = 2;
export declare const copyLinkContainerStyle: {
    cursor: string;
    '& #copy-link-button': {
        opacity: number;
        transition: string;
    };
    '&:hover #copy-link-button': {
        opacity: number;
        transition: string;
    };
};
