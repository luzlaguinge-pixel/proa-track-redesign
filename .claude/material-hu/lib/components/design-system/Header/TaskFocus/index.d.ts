import { type TaskFocusHeaderProps } from './types';
declare const TaskFocusHeader: ({ title, pillLabel, pillProps, mainActions, extraActions, onClose, onBack, slotProps, copyLinkButton, loading, }: TaskFocusHeaderProps) => import("react/jsx-runtime").JSX.Element;
export type { TaskFocusHeaderProps };
export default TaskFocusHeader;
