import { type SocialHeaderProps } from './types';
declare const SocialHeader: ({ avatar, title, pillLabel, mainActions, extraActions, onClose, onBack, slotProps, copyLinkButton, loading, }: SocialHeaderProps) => import("react/jsx-runtime").JSX.Element;
export type { SocialHeaderProps };
export default SocialHeader;
