import { type StateCardProps } from './types';
declare const StateCard: ({ title, description, primaryAction, secondaryAction, variant, icon, slotProps, }: StateCardProps) => import("react/jsx-runtime").JSX.Element;
export type { StateCardProps };
export default StateCard;
