import { type ChipProps } from './types';
declare const Chip: ({ label, size, disabled, onDelete, isSelected, onClick, sx, slotProps, }: ChipProps) => import("react/jsx-runtime").JSX.Element;
export type { ChipProps };
export default Chip;
