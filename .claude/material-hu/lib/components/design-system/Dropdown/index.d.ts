import { type DropdownProps } from './types';
declare const Dropdown: ({ children, label, open, onOpen, onClose, onClick, buttonVariant, position, buttonSize, hasIcon, sx, buttonProps, }: DropdownProps) => import("react/jsx-runtime").JSX.Element;
export type { DropdownProps };
export default Dropdown;
