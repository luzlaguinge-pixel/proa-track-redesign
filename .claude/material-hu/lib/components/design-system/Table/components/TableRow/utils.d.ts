import { type Theme } from '@mui/material';
export declare const getBackgroundColor: (headerRow: boolean, selected: boolean | undefined, theme: Theme) => {
    backgroundColor: string;
    '&:hover': {
        backgroundColor: string;
    };
};
