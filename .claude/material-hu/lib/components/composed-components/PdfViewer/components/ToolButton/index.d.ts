import { type ToolButtonProps } from './types';
export declare const ToolButton: ({ label, Icon, onClick, disabled, }: ToolButtonProps) => import("react/jsx-runtime").JSX.Element;
export default ToolButton;
