import { type PdfFullScreenProps } from './types';
export declare const PdfFullScreen: ({ file, name, onClose, showHeader, slotProps, fileProps, }: PdfFullScreenProps) => import("react/jsx-runtime").JSX.Element;
export default PdfFullScreen;
