import { type HeaderProps } from './types';
declare const Header: ({ name, hideName, onClose, hideClose, onDownload, hideDownload, }: HeaderProps) => import("react/jsx-runtime").JSX.Element;
export default Header;
