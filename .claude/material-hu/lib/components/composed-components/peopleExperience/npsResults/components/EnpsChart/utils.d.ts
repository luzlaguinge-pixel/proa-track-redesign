import { type Theme } from '@mui/material/styles';
import { type EnpsChartDataItem } from './types';
export declare const generateNullAnnotations: (data: EnpsChartDataItem[], theme: Theme, message: string) => Record<string, any>;
