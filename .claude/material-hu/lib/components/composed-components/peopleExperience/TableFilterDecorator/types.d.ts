import { type ReactNode } from 'react';
export type TableFilterDecoratorProps = {
    backgroundColor?: string;
    textColor?: string;
    title: string;
    description?: string;
    icon?: ReactNode;
};
