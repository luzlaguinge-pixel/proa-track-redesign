import { type PopoverProps } from '@mui/material';
declare const ScoreComparisonPopover: ({ score, baseScore, difference, ...props }: PopoverProps & {
    score: number;
    baseScore: number;
    difference: number;
}) => import("react/jsx-runtime").JSX.Element;
export default ScoreComparisonPopover;
