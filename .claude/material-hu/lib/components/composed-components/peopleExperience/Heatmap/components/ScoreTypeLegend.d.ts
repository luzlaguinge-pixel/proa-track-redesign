import { ScoreType } from '../../types';
type ScoreTypeLegendProps = {
    scoreTypeId: ScoreType;
};
declare const ScoreTypeLegend: ({ scoreTypeId }: ScoreTypeLegendProps) => import("react/jsx-runtime").JSX.Element | undefined;
export default ScoreTypeLegend;
