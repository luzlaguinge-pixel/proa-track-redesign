import { type TimePickerProps } from './types';
/**
 * @deprecated Use InputTime instead. This component will be removed in the next major version.
 */
declare const TimePicker: ({ value, disabled, error, size, label, helperText, errorText, fullWidth, sx, onChange, minutesStep, placeholder, timezone, referenceDate, disablePast, disableFuture, minTime, maxTime, noIcon, ...props }: TimePickerProps) => import("react/jsx-runtime").JSX.Element;
export default TimePicker;
