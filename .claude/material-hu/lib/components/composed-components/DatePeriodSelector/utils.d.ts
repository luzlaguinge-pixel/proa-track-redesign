import { Periods } from './types';
export declare const recognizePeriod: (fromDate: Date, toDate: Date) => Periods;
export declare const checkDateReference: (currentDate: Date, date: Date) => string;
