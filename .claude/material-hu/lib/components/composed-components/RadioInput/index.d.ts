import { type RadioInputProps } from './types';
declare const RadioInput: ({ name, options, value, onChange, label, error, success, disabled, slotProps, sx, }: RadioInputProps) => import("react/jsx-runtime").JSX.Element;
export default RadioInput;
