import { type StateCardProps } from './types';
export declare const StateCard: ({ sx, slotProps, cardContained, color, Icon, action, variant, title, description, }: StateCardProps) => import("react/jsx-runtime").JSX.Element;
export type { StateCardProps };
export default StateCard;
