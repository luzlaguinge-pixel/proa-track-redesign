import { type PieProps } from './types';
declare const Pie: ({ copetin, title, description, items, sx, slotProps, legend, loading, }: PieProps) => import("react/jsx-runtime").JSX.Element;
export default Pie;
