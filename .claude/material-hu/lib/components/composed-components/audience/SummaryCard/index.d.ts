import { type SummaryCardProps } from './types';
declare const SummaryCard: ({ Icon, title, description, onEdit, onDelete, loading, disabled, sx, slotProps, }: SummaryCardProps) => import("react/jsx-runtime").JSX.Element;
export default SummaryCard;
