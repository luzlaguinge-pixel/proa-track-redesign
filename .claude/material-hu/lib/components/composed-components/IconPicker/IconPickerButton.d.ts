import { type IconPickerButtonProps } from './types';
declare const IconPickerButton: ({ value, open, onClick, buttonRef, disabled, sx, renderIcon, }: IconPickerButtonProps) => import("react/jsx-runtime").JSX.Element;
export default IconPickerButton;
