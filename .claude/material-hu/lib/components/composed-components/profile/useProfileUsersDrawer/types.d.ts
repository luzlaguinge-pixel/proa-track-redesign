export type User = {
    name: string;
    profilePicture?: string;
};
export type Texts = {
    collaborator_count: string;
    drawer_title: string;
    no_items_found: string;
    back: string;
    items_total: string | JSX.Element;
};
