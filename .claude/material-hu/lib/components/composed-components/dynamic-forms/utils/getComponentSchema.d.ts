import { type DynamicFormComponent, type ErrorTextsDefinition } from '../types';
/**
 * Function to get the component validationschema
 * @returns The validation schema for the component
 */
export declare const getComponentSchema: (component: DynamicFormComponent, errorTexts: ErrorTextsDefinition) => any;
