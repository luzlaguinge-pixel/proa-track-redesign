import { type DynamicFormBody, type GetDynamicFormBodyProps } from '../types';
/**
 * Function to get the dynamic form body
 * @returns The dynamic form body
 */
export declare const getDynamicFormBody: ({ sectionName, fieldValues, sectionEvent, components, fillingTime, }: GetDynamicFormBodyProps) => DynamicFormBody;
