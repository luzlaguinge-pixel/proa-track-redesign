export type VitePluginConfig = {
    enabled: boolean;
};
export type TsConfig = Record<string, unknown>;
export type FolderPathParams = {
    enabled: boolean;
    folderPath: string;
    repoName: string;
};
